<nav class="bg-slate-800/95 backdrop-blur-sm border-b border-slate-700/50 mb-8 sticky top-0 z-50 shadow-lg">
    <div class="container mx-auto px-4">
        <div class="flex items-center justify-between h-16">
            <!-- Brand -->
            <a class="text-xl font-bold text-white flex items-center gap-2 hover:scale-105 transition-transform duration-200" href="/">
                <div class="p-2 bg-gradient-to-br from-blue-500 to-blue-600 rounded-lg shadow-lg shadow-blue-500/30">
                    <i class="fas fa-chart-line text-white"></i>
                </div>
                <span class="bg-gradient-to-r from-blue-400 to-blue-500 bg-clip-text text-transparent">Diário</span>
            </a>

            <!-- Mobile Menu Button -->
            <button id="mobile-menu-btn" class="md:hidden text-slate-300 hover:text-white focus:outline-none focus:ring-2 focus:ring-blue-500 rounded-lg p-2 transition-colors">
                <i id="menu-icon" class="fas fa-bars text-2xl"></i>
            </button>

            <!-- Desktop Menu -->
            <div class="hidden md:flex items-center space-x-6">
                <% if (user) { %>
                    <a class="group text-slate-300 hover:text-white px-3 py-2 rounded-lg text-sm font-medium transition-all duration-200 flex items-center gap-2 hover:bg-slate-700/50" href="/assets">
                        <div class="p-1.5 bg-emerald-500/10 rounded-md group-hover:bg-emerald-500/20 transition-colors">
                            <i class="fas fa-wallet text-emerald-400 text-sm"></i>
                        </div>
                        <span>Ativos</span>
                    </a>
                    <a class="group text-slate-300 hover:text-white px-3 py-2 rounded-lg text-sm font-medium transition-all duration-200 flex items-center gap-2 hover:bg-slate-700/50" href="/transactions">
                        <div class="p-1.5 bg-amber-500/10 rounded-md group-hover:bg-amber-500/20 transition-colors">
                            <i class="fas fa-exchange-alt text-amber-400 text-sm"></i>
                        </div>
                        <span>Transações</span>
                    </a>
                <% } %>
            </div>

            <!-- Right Side (User/Auth) -->
            <div class="hidden md:flex items-center space-x-4">
                <% if (user) { %>
                    <div class="relative group">
                        <button class="flex items-center gap-2 text-slate-300 hover:text-white focus:outline-none px-3 py-2 rounded-lg hover:bg-slate-700/50 transition-all duration-200">
                            <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full flex items-center justify-center text-white font-semibold text-sm shadow-lg">
                                <%= user.name.charAt(0).toUpperCase() %>
                            </div>
                            <span class="font-medium"><%= user.name %></span>
                            <i class="fas fa-chevron-down text-xs transition-transform duration-200 group-hover:rotate-180"></i>
                        </button>
                        <!-- Dropdown -->
                        <div class="absolute right-0 mt-2 w-56 bg-slate-800 border border-slate-700 rounded-xl shadow-2xl py-2 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50 transform group-hover:translate-y-0 -translate-y-2">
                            <div class="px-4 py-3 border-b border-slate-700">
                                <p class="text-sm text-slate-400">Conectado como</p>
                                <p class="text-sm font-medium text-white truncate"><%= user.email %></p>
                            </div>
                            <a href="/auth/profile" class="flex items-center gap-3 px-4 py-2.5 text-sm text-slate-300 hover:bg-slate-700/50 hover:text-white transition-colors">
                                <i class="fas fa-user-circle text-blue-400 w-5"></i>
                                <span>Meu Perfil</span>
                            </a>
                            <div class="border-t border-slate-700 my-1"></div>
                            <a href="/auth/logout" class="flex items-center gap-3 px-4 py-2.5 text-sm text-red-400 hover:bg-red-500/10 hover:text-red-300 transition-colors">
                                <i class="fas fa-sign-out-alt w-5"></i>
                                <span>Sair</span>
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    <a class="text-slate-300 hover:text-white font-medium px-4 py-2 rounded-lg hover:bg-slate-700/50 transition-all duration-200" href="/auth/login">Login</a>
                    <a class="bg-gradient-to-r from-blue-600 to-blue-500 hover:from-blue-500 hover:to-blue-400 text-white px-5 py-2 rounded-lg font-medium transition-all duration-200 shadow-lg shadow-blue-500/30 hover:shadow-blue-500/50 hover:scale-105 inline-block" href="/auth/register">Registrar</a>
                <% } %>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="md:hidden hidden overflow-hidden transition-all duration-300 ease-in-out">
            <div class="px-2 pt-2 pb-4 space-y-2 border-t border-slate-700 mt-2">
                <% if (user) { %>
                    <!-- User Info Mobile -->
                    <div class="px-3 py-3 bg-slate-700/30 rounded-lg mb-3">
                        <div class="flex items-center gap-3">
                            <div class="w-10 h-10 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full flex items-center justify-center text-white font-semibold shadow-lg">
                                <%= user.name.charAt(0).toUpperCase() %>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-white"><%= user.name %></p>
                                <p class="text-xs text-slate-400 truncate"><%= user.email %></p>
                            </div>
                        </div>
                    </div>
                    
                    <a class="flex items-center gap-3 text-slate-300 hover:text-white hover:bg-slate-700/50 px-3 py-2.5 rounded-lg text-sm font-medium transition-all" href="/assets">
                        <div class="p-1.5 bg-emerald-500/10 rounded-md">
                            <i class="fas fa-wallet text-emerald-400"></i>
                        </div>
                        <span>Ativos</span>
                    </a>
                    <a class="flex items-center gap-3 text-slate-300 hover:text-white hover:bg-slate-700/50 px-3 py-2.5 rounded-lg text-sm font-medium transition-all" href="/transactions">
                        <div class="p-1.5 bg-amber-500/10 rounded-md">
                            <i class="fas fa-exchange-alt text-amber-400"></i>
                        </div>
                        <span>Transações</span>
                    </a>
                    <a class="flex items-center gap-3 text-slate-300 hover:text-white hover:bg-slate-700/50 px-3 py-2.5 rounded-lg text-sm font-medium transition-all" href="/auth/profile">
                        <div class="p-1.5 bg-blue-500/10 rounded-md">
                            <i class="fas fa-user-circle text-blue-400"></i>
                        </div>
                        <span>Meu Perfil</span>
                    </a>
                    <div class="border-t border-slate-700 my-2"></div>
                    <a class="flex items-center gap-3 text-red-400 hover:text-red-300 hover:bg-red-500/10 px-3 py-2.5 rounded-lg text-sm font-medium transition-all" href="/auth/logout">
                        <i class="fas fa-sign-out-alt"></i>
                        <span>Sair</span>
                    </a>
                <% } else { %>
                    <a class="block text-slate-300 hover:text-white hover:bg-slate-700/50 px-3 py-2.5 rounded-lg text-sm font-medium transition-all" href="/auth/login">Login</a>
                    <a class="block bg-gradient-to-r from-blue-600 to-blue-500 hover:from-blue-500 hover:to-blue-400 text-white px-3 py-2.5 rounded-lg font-medium text-center transition-all shadow-lg shadow-blue-500/30" href="/auth/register">Registrar</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>

<script>
    // Mobile menu toggle
    const mobileMenuBtn = document.getElementById('mobile-menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');
    const menuIcon = document.getElementById('menu-icon');
    
    if (mobileMenuBtn && mobileMenu) {
        mobileMenuBtn.addEventListener('click', function() {
            mobileMenu.classList.toggle('hidden');
            
            // Toggle icon
            if (mobileMenu.classList.contains('hidden')) {
                menuIcon.classList.remove('fa-times');
                menuIcon.classList.add('fa-bars');
            } else {
                menuIcon.classList.remove('fa-bars');
                menuIcon.classList.add('fa-times');
            }
        });
    }
</script>
